function [meanBx,meanBy,meanBz] = MagneticMean(date_start,date_end,fgm_timedata,fgm_bdata)
    %using magnetic field survey data, calculate the mean field components
    %before/after the event up to two minutes. 
    outputArg1 = inputArg1;
    outputArg2 = inputArg2;
end

